"use client";
import {useState} from "react";
import Link from "next/link";
import {Menu,X} from "lucide-react";

export function MobileMenu(){const [open,setOpen]=useState(false);return <div className="mobile-menu"><button className="menu-button" onClick={()=>setOpen(!open)} aria-label="Open menu">{open?<X/>:<Menu/>}</button>{open&&<div className="mobile-drawer"><Link onClick={()=>setOpen(false)} href="/features">Product</Link><Link onClick={()=>setOpen(false)} href="/how-it-works">How it works</Link><Link onClick={()=>setOpen(false)} href="/parking-scanner">AI Parking Scanner</Link><Link onClick={()=>setOpen(false)} href="/parking-map">Parking Map</Link><Link onClick={()=>setOpen(false)} href="/vehicle-awareness">Vehicle Awareness</Link><Link onClick={()=>setOpen(false)} href="/safety">Safety</Link><Link onClick={()=>setOpen(false)} href="/support">Support</Link><Link onClick={()=>setOpen(false)} href="/download" className="btn btn-primary">Get NaviParka</Link></div>}</div>}

export function CookieConsent(){const [show,setShow]=useState(true);const [manage,setManage]=useState(false);const [analytics,setAnalytics]=useState(false);if(!show)return null;return <div className="cookie-wrap"><div className="cookie card">{!manage?<><div><b>Your privacy matters</b><p>We use essential technologies to operate NaviParka. With your permission, analytics can help us understand how the website is used.</p></div><div className="cookie-actions"><button className="btn btn-primary" onClick={()=>{setAnalytics(true);setShow(false)}}>Accept analytics</button><button className="btn btn-secondary" onClick={()=>setShow(false)}>Essential only</button><button className="btn btn-secondary" onClick={()=>setManage(true)}>Manage</button></div></>:<><div><b>Cookie preferences</b><div className="preference-row"><span><strong>Essential</strong><small>Required for the website to operate.</small></span><b>Always on</b></div><label className="preference-row"><span><strong>Analytics</strong><small>Help us understand website usage.</small></span><input type="checkbox" checked={analytics} onChange={e=>setAnalytics(e.target.checked)}/></label></div><div className="cookie-actions"><button className="btn btn-primary" onClick={()=>setShow(false)}>Save preferences</button><button className="btn btn-secondary" onClick={()=>setManage(false)}>Back</button></div></>}</div></div>}
