import {PageHero,PhoneMockup,MapMockup} from "@/components/site";import Link from "next/link";import {ArrowRight} from "lucide-react";
const cards=[{title:"No scanning mode to choose",body:"Point your camera at the evidence. NaviParka handles the interpretation flow."},{title:"Signs + road markings",body:"A parking situation can contain more than one useful clue, so the product is designed around evidence rather than a single object type."},{title:"Decision-first results",body:"A simple parking verdict is prioritised before the detailed explanation."},{title:"Responsible uncertainty",body:"Obscured signs, poor lighting, damaged markings, conflicting information and temporary restrictions can affect what the system can understand."}];
export default function Page(){return <><PageHero eyebrow="AI Parking Scanner" title="Parking evidence in. Clear guidance out." body="NaviParka is designed to understand the parking evidence in front of you without asking you to pick a scan type first."><div className="actions"><Link href="/download" className="btn btn-primary">Get NaviParka <ArrowRight size={16}/></Link></div></PageHero><section className="section" style={{paddingTop:30}}><div className="container"><div className="pagegrid">{cards.map((c,i)=><article className="card pagecard" key={c.title}><div className="feature-number">{String(i+1).padStart(2,'0')}</div><h2 className="h3" style={{marginTop:30}}>{c.title}</h2><p className="lead" style={{fontSize:16}}>{c.body}</p></article>)}</div><div style={{marginTop:50}}><PhoneMockup kind="scanner"/></div></div></section></>}