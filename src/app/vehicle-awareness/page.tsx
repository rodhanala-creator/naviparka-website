import {PageHero,PhoneMockup,MapMockup} from "@/components/site";import Link from "next/link";import {ArrowRight} from "lucide-react";
const cards=[{title:"Saved vehicle profile",body:"Store registration, vehicle details and fuel type to personalise relevant parking interpretations."},{title:"EV-aware restrictions",body:"Explain EV-only bays and active-charging requirements in the context of the vehicle being driven."},{title:"Unknown vehicle",body:"When the vehicle context is missing, NaviParka can ask a simple question such as \u201cAre you driving an electric vehicle?\u201d"},{title:"Context, not assumptions",body:"Vehicle information improves relevant guidance without pretending every parking rule can be inferred from the vehicle alone."}];
export default function Page(){return <><PageHero eyebrow="Vehicle Awareness" title="The right answer can depend on your vehicle." body="Vehicle information can make parking guidance more relevant when a restriction depends on fuel type or eligibility."><div className="actions"><Link href="/download" className="btn btn-primary">Get NaviParka <ArrowRight size={16}/></Link></div></PageHero><section className="section" style={{paddingTop:30}}><div className="container"><div className="pagegrid">{cards.map((c,i)=><article className="card pagecard" key={c.title}><div className="feature-number">{String(i+1).padStart(2,'0')}</div><h2 className="h3" style={{marginTop:30}}>{c.title}</h2><p className="lead" style={{fontSize:16}}>{c.body}</p></article>)}</div></div></section></>}